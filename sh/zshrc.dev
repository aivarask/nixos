#!/usr/bin/env zsh

typeset -TU NIX_PROFILES np ' '
typeset -TU GTK_PATH gtk_path ':'
typeset -TU XCURSOR_PATH xcursor_path ':'


echo ${0}
echo ${0:a}
echo ${0:a:h}/bin

echo $(readlink $0)
echo ${$(readlink $0):h}

echo $SELF
