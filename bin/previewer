#!/usr/bin/env bash

arg=$1
# shift
one=$(echo "$arg" | awk '{print $1}')
command systemctl status "$one"

# case "$command" in
# 	cd)           fzf --preview 'tree -C {} | head -200'   "$@" ;;
# 	export|unset) fzf --preview "eval 'echo \$'{}"         "$@" ;;
# 	ssh)          fzf --preview 'dig {}'                   "$@" ;;
# 	*)            fzf --preview 'bat -n --color=always {}' "$@" ;;
# esac
