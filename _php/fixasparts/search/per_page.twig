<form hx-get="/search/"
  hx-trigger="change from:#per_page"
  style="display:flex; align-items: center;">
  <select id="per_page" name="per_page" title="Viso rasta: {{ total }}">
    <option {{ isSelected('50', perPage) }} value="50">
      50/{{ total }}
    </option>
    <option {{ isSelected('100', perPage) }} value="100">
      100/{{ total }}
    </option>
    <option {{ isSelected('200', perPage) }} value="200">
      200/{{ total }}
    </option>
    <option {{ isSelected('500', perPage) }} value="500">
      500/{{ total }}
    </option>
  </select>
</form>
