# fs
  -- vim.fs.find(file) -- option argument required
  -- vim.fn.glob -- pattern based
  -- exists = io.open(file) -- probably buffer returned
  -- vim.fn.findfile -- path optional required
  -- vim.uv -- does not have typings builtin
  -- require('plenary.path') -- not tried yet

