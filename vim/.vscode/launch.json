{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "pwa-node",
      "request": "launch",
      "name": "Launch file",
      "program": "${file}",
      "cwd": "${workspaceFolder}",
      "env": {
        "APP_FOO": "bar",
        "NODE_ENV": "test"
      }
    }
  ]
}
