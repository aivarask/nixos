const foo = "bara";
const person = {
  name: "Alice",
  age: 30,
};

console.log(foo);
console.log(person.name);

const array = [1, 2, 3, 4];
for (let index = 0; index < array.length; index++) {
  const element = array[index];
  console.log(element);
}
